Current tournament setting is a bit two complex.

Lets separate simple tournaments with one or two disciplines with large actions.

When creating a new tournament after the first modal, i should get second, lets call it tournament mode:

Radios:
() Easy mode - simple tournament, no advanced feature
() Advanced mode - one or more of
  [ ] tournament schedule (disciplines specifies where and when they happens)
     i - for larger tournaments with multiple disciplines in different days and locations
  [ ] payments
     i - i want to use hema squire to handle payments agenda 
  [ ] 