1. Tournament creation
  - having two modals for tournament creation is a bit clumsy — integrate the second into the first while keeping
    it nice and tidy
2. Hints popup
  - the hints popup under the "i" is wider than the window, so part of it is invisible and the scrollbar shakes
    the content
  - place the popup center-bottom instead of to the right of the "i"
