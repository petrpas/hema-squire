1. Settings
  - bug: disciplines name and slug hint popup is hidden behind other column names - bring it to the top  
  - bug: disciplines modal when editing existing does not contain actual values of name and slug
2. Preview
  - reg. form 
    - place bigger v space before registration instructions and before total price, also align total price to the right of the table
    - there seems to be some dangling fields like the box with post tournement sparring and some note to accomodation 
      - this should be removed 
3. Tournament detail
   - tournament logo 2x larger and no borders
   - boxes are stacket together - should be some space between them
   - the page is not scrolling, bottom half is invisible 
   - too much is going on at this page, add second tab [Registered] if fencer already registered if not [Register]
   - "Back to tournaments" - remove and put X mark on the top right corner next to the tournament name
     - e.g. TTT 2027 <-----> [Registered] [X] 
   - to return to the detail page change the button to [Tournament]
