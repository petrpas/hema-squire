1. Settings:
 - the small panel above Status panel is transparent and scrolling form is to be seen there. 
 - tournament tab:
   - save button does not work for description, registration instructions or qualification, check it for any field   
   - same for the organizer list - remove ok, adding is ignored
- disciplines tab
  - changing the where, when, rules, and link triggers the price change warning. this should happen only for price change  
- extra tab
  - strange behavior of + button, seems to be confirming current line instead of addin another option (same for disciplines)
    - better behavior - use the + Add discipline / item as the discounts have 
- payments tab 
  - saving does not work here either
2. Preview  
  - torunament
    - currently, where, when, rules and link appears in the same line as price which destroys the layout  
    - if any of those optional fields is shown an extra line with bullet should appear bellow the discipline line with the info:
    - also put extra space around the dots A · B -> A   ·   B (e.g. space + nbsp + space or some other trick) 
      - e.g. LS — Longsword Open       950 Kč (38 €)   ·   1/32 fencers
                - Saturday   ·   Main Hall   ·   Ruleset: RoW
  - registration
    - extra spaces around dots as well 
  