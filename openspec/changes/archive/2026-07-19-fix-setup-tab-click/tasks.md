# Tasks: fix-setup-tab-click

## 1. Fix

- [x] 1.1 Change `.step-slot:first-child` from `flex: 0` to `flex: none` in `frontend/src/index.css`
- [x] 1.2 Verify: `npm run build` clean; visually confirm in the dev server that the Setup tab is clickable from every phase and the stepper alignment holds at normal and narrow widths
