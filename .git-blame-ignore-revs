# Revisions to skip when reading blame — commits that moved code without
# changing it. Point git at this file once, per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# (GitHub, GitLab and the JetBrains IDEs read it without being told.)

# Formatting only: Biome's reflow of the frontend
ec30447a6390e1b4e985fa9d9e12048150d94263
